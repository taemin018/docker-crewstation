<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crewstation.mapper.diary.diary.path.DiaryDiaryPathMapper">
    <insert id="insert">
        insert into tbl_diary_diary_path(diary_path_id, post_id)
        values (#{diaryPathId},#{postId})
    </insert>
</mapper>
