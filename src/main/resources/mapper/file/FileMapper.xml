<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crewstation.mapper.file.FileMapper">
    <insert id="insertFile"  useGeneratedKeys="true" keyProperty="id">
        insert into tbl_file (file_origin_name, file_path, file_name, file_size)
        values (#{fileOriginName}, #{filePath}, #{fileName}, #{fileSize})
    </insert>

    <insert id="insert"  useGeneratedKeys="true" keyProperty="id">
        insert into tbl_file (file_origin_name, file_path, file_name, file_size)
        values (#{fileOriginName}, #{filePath}, #{fileName}, #{fileSize})
    </insert>

    <delete id="delete">
        delete from tbl_file
        where id = #{id}
    </delete>

    <select id="selectOne">
        select id,
               file_path as filePath,
               file_name as fileName,
               file_origin_name as fileOriginName,
               file_size as fileSize
        from tbl_file
        where id = #{id}
    </select>

    <select id="findProfileByMemberId">
        SELECT f.id,
               f.file_path AS filePath,
               f.file_name AS fileName,
               f.file_origin_name AS fileOriginName,
               f.file_size AS fileSize
        FROM tbl_member_file mf
                 JOIN tbl_file f ON mf.file_id = f.id
        WHERE mf.member_id = #{memberId}
        LIMIT 1
    </select>

</mapper>