<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crewstation.mapper.report.ReportMapper">
    <insert id="insertReport" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_report (report_content, member_id)
        values (#{reportContent},#{memberId})
    </insert>

    <insert id="insertReportPost">
        insert into tbl_post_report(report_id, post_id)
        values (#{reportId},#{postId})
    </insert>

    <insert id="insertReportReply">
        insert into tbl_reply_report(report_id, reply_id)
        values (#{reportId},#{replyId})
    </insert>
</mapper>