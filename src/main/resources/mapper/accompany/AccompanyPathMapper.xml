<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.crewstation.mapper.accompany.AccompanyPathMapper">

    <select id="findCountryPath">
        select
            tap.country_start_date,
            tap.country_end_date,
            c.country_name


        from tbl_accompany_path tap
            join tbl_country c
                on tap.country_id = c.id

    </select>
</mapper>